query GetSupplierQuery($id: String!) {
    supplier(id: $id) {
        ... on Supplier {
            id
            name
        }
        ... on ErrorResponse {
            type
            message
        }
    }
}